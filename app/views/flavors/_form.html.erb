<%= form_with(model: flavor, local: true) do |form| %>
  <% if flavor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flavor.errors.count, "error") %> prohibited this flavor from being saved:</h2>

      <ul>
      <% flavor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--form attributes-->
<div class="form-row">
  
      <div class="form-group col-md-4">
      <label for="name"><%= form.label :name %></label>
      <%= form.text_field :name, :class => "form-control" %>
      </div>

</div>

<div class="form-row">
  
  <div class="form-group col-md-2">
    <label for="active"><%= form.label :active %></label>
    <%= form.text_field :active, :class => "form-control" %>
  </div>
  
    
</div>


<%= form.submit "Submit", :class => 'btn btn-primary' %>
<br/>

<br/>
<% end %>
